import 'package:gurps_character_creation/models/traits/trait.dart';

class Advantage extends Trait {
  Advantage(
      {required super.name,
      required super.description,
      required super.traitType,
      required super.cost});
}
